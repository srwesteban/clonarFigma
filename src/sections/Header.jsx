import React from 'react';

const Header = () => {
  return (
    <header className='flex flex-row py-4 px-6 bg-portfolio-primary-600 shadow-2xl'>
      <span>logo</span>
      <nav>nav</nav>
      <section>buttons</section>
    </header>
  );
};

export default Header;
